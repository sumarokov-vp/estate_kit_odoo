services:
  odoo-arm:
    build:
      context: ..
      dockerfile: build/Dockerfile
    image: docker.io/sumarokovvp/simplelogic:estate_kit_arm

  odoo-amd64:
    build:
      context: ..
      dockerfile: build/Dockerfile
      platforms:
        - linux/amd64
    image: docker.io/sumarokovvp/simplelogic:estate_kit_amd64
